name: teemates
description: A golf app with main menu and login
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.17.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15
  flutter_secure_storage: ^8.0.0
  sign_in_with_apple: ^5.0.0
  google_sign_in: ^6.1.5
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
  assets:
    - lib/golf_courses_sweden.json
